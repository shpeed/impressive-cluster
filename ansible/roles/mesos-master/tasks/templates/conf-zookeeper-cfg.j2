tickTime={{tick_time}}
dataDir={{data_dir}}
clientPort={{zookeeper_client_port}}
initLimit={{init_limit}}
syncLimit={{sync_limit}}

{% for host in zookeeper_ids.keys() -%}
  server.{{ zookeeper_ids[host] }}={{ hostvars[host]['ansible_eth1']['ipv4']['address'] }}:{{ zookeeper_server_port }}:{{ zookeeper_election_port }}
{% endfor %}